# Render runs builds with NODE_ENV=production, which makes npm omit
# devDependencies. typescript and @types/node are devDependencies, so without
# this line `npm ci` installs 27 packages instead of 61 and `tsc` is missing,
# which fails the build with "Cannot find name 'node:http'".
include=dev
